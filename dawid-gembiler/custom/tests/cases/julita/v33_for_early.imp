# Advanced Test 33: Mixed FOR and WHILE with early exit simulation
PROGRAM IS
  sum, found
IN
  sum := 0;
  found := 0;
  FOR i FROM 1 TO 100 DO
    sum := sum + i;
    IF sum > 500 THEN
      IF found = 0 THEN
        WRITE i;
        found := 1;
      ENDIF
    ENDIF
  ENDFOR
  WRITE sum;
END
