PROCEDURE set(T arr, idx, val) IS
IN
    arr[idx] := val;
    val := val * 2;
END

PROGRAM IS
    tab[0:5], z, f, one
IN
    z := 0;
    f := 5;
    one := 111;
    set(tab, z, one);
    set(tab, f, one);
    WRITE tab[0];
    WRITE tab[5];
END
